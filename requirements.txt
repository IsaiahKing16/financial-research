# FPPE - Dependencies
#
# ENVIRONMENT NOTE (Windows):
#   The venv at financial-research/venv is WSL-origin (Python 3.10, pyvenv.cfg → /usr/bin)
#   and cannot be activated from Windows shells. Use the system Python 3.12 directly:
#
#     python -m pip install -r requirements.txt
#     python -m pip install -r requirements-dev.txt
#
#   scipy and numpy are pre-installed system-wide. `pot` installs to user site-packages
#   (%APPDATA%\Python\Python312\site-packages) which Python 3.12 picks up automatically.

# Data
yfinance>=0.2.36          # US stock data (no API key needed)
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0           # Parquet file support (fast data storage)

# Technical indicators (supplementary features)
ta>=0.11.0

# Machine learning
scikit-learn>=1.3.0       # NearestNeighbors, StandardScaler, metrics
optuna>=3.4.0             # Bayesian hyperparameter optimization (TPE)
scipy>=1.6.0              # Wasserstein distance fallback, t-distribution (BMA)

# Research modules (Phase 3.5)
pot>=0.9.0                # Python Optimal Transport — ot.emd2 for EMDDistance

# Visualization
matplotlib>=3.7.0

# Utilities
requests>=2.31.0
tqdm>=4.65.0
joblib>=1.3.0

# Phase 3Z: Contract validation
pydantic>=2.0.0           # Frozen Pydantic models for cross-module contracts
pandera>=0.18.0           # DataFrame schema validation (strict=True, coerce=False)
hnswlib>=0.7.0            # HNSW approximate nearest-neighbor index (Matcher backend)

# Phase 1: BSS calibration experiments
betacal>=0.1.0            # Beta calibration (Kull et al. 2017, AISTATS) — H4 experiment