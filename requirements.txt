# System A: Quant Engine - Dependencies
# Install: pip install -r requirements.txt
#
# NOTE: TensorFlow is NO LONGER REQUIRED.
# The analogue matching approach uses scikit-learn's NearestNeighbors
# which is pure CPU and runs fast on any hardware.

# Data
yfinance>=0.2.36          # US stock data (no API key needed)
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0           # Parquet file support (fast data storage)

# Technical indicators (supplementary features)
ta>=0.11.0

# Machine learning
scikit-learn>=1.3.0       # NearestNeighbors, StandardScaler, metrics

# Visualization
matplotlib>=3.7.0

# Utilities
requests>=2.31.0
tqdm>=4.65.0
joblib>=1.3.0