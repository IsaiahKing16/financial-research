# Python
__pycache__/
*.py[cod]
*.egg-info/
venv/
.venv/

# Data (large files — don't commit)
data/*.csv
data/*.npz
data/*.pkl
data/*.parquet

# Results (generated outputs — regenerate, don't track)
results/*.csv
results/*.tsv
results/*.txt
results/*.json

# Models (large files)
models/*.keras
models/*.h5
models/*.pkl

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# TensorFlow (legacy, no longer used)
*.tfevents.*
logs/

# Lock file is tracked; this comment is for clarity
# requirements.lock.txt — tracked intentionally
